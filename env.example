# ============================================================================
# AYUBO CAFE - ENVIRONMENT VARIABLES
# ============================================================================
#
# Copy this file to `.env` and update with your actual values
# Command: cp env.example .env
#
# ⚠️ IMPORTANT: Never commit .env file to git (already in .gitignore)
#
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Example:
# VITE_SUPABASE_URL=https://abcdefghijklm.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# EMAIL CONFIGURATION (Optional - for password reset emails)
# ============================================================================
# Leave blank to use development mode (emails logged to console)
# Configure for production to send actual emails

# ===== Gmail SMTP (Recommended) =====
# Follow setup guide: EMAIL_SETUP_GUIDE.md

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-character-app-password
EMAIL_FROM="Ayubo Cafe <your-email@gmail.com>"

# Enable email sending (set to 'true' to send real emails, 'false' for console logging)
# Note: Must use VITE_ prefix for Vite to expose to client
VITE_EMAIL_ENABLED=false

# Debug mode: Set to 'true' to see full email content in console (even when VITE_EMAIL_ENABLED=true)
VITE_EMAIL_DEBUG=false

# Application URL (used for generating password reset links)
# Development: http://localhost:3000
# Production: https://yourdomain.com
VITE_APP_URL=http://localhost:3000

# ===== Alternative SMTP Services =====

# SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

# Mailgun:
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-mailgun-username
# EMAIL_PASSWORD=your-mailgun-password

# AWS SES:
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-ses-smtp-username
# EMAIL_PASSWORD=your-ses-smtp-password

# ============================================================================
# SMS CONFIGURATION (Optional - for customer OTP verification)
# ============================================================================
# Leave blank to use development mode (SMS logged to console)
# Configure for production to send actual SMS messages

# Enable SMS sending (set to 'true' to send real SMS, 'false' for console logging)
VITE_SMS_ENABLED=false

# SMS Webhook URL (your SMS gateway webhook endpoint)
# Examples: 
# - Twilio: https://api.twilio.com/2010-04-01/Accounts/YOUR_ACCOUNT_SID/Messages.json
# - Vonage/Nexmo: https://rest.nexmo.com/sms/json
# - Custom webhook: https://your-sms-gateway.com/api/send
VITE_SMS_WEBHOOK_URL=https://your-sms-webhook-url.com/api/send

# SMS Webhook Authentication Key/Token
# This will be sent as Authorization: Bearer TOKEN
VITE_SMS_WEBHOOK_KEY=your-webhook-api-key

# From Number or Sender ID
# For Sri Lankan numbers: +94XXXXXXXXX
# For Sender ID (if supported): AYUBOCAFE (max 11 chars, alphanumeric)
VITE_SMS_FROM_NUMBER=Ayubo Cafe

# Debug mode: Set to 'true' to see full SMS content in console
VITE_SMS_DEBUG=false

# ===== Popular SMS Gateway Options for Sri Lanka =====

# Dialog Ideabiz SMS Gateway:
# VITE_SMS_WEBHOOK_URL=https://api.dialog.lk/sms/send
# VITE_SMS_WEBHOOK_KEY=your-dialog-api-key

# Twilio (International):
# VITE_SMS_WEBHOOK_URL=https://api.twilio.com/2010-04-01/Accounts/YOUR_ACCOUNT_SID/Messages.json
# VITE_SMS_WEBHOOK_KEY=your-twilio-auth-token
# Note: Twilio requires Account SID and Auth Token

# Vonage/Nexmo (International):
# VITE_SMS_WEBHOOK_URL=https://rest.nexmo.com/sms/json
# VITE_SMS_WEBHOOK_KEY=your-nexmo-api-key

# ============================================================================
# STRIPE PAYMENT CONFIGURATION (Optional - for online payments)
# ============================================================================
# Leave blank to use bank transfer only
# Configure for production to accept online card payments

# Stripe Publishable Key (safe to expose in client-side code)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Secret Key (keep this secure, used in Netlify functions only)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe Webhook Secret (for verifying webhook signatures)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Enable Stripe payments
VITE_STRIPE_ENABLED=false

# ============================================================================
# NOTES
# ============================================================================
#
# 1. SUPABASE:
#    - Required for database operations
#    - Get credentials from Supabase dashboard
#    - VITE_ prefix required for Vite to expose to client
#
# 2. EMAIL:
#    - Optional for development (set VITE_EMAIL_ENABLED=false)
#    - Required for production password reset (set VITE_EMAIL_ENABLED=true)
#    - Gmail setup: See EMAIL_SETUP_GUIDE.md
#    - Console logging mode: Emails logged to console (VITE_EMAIL_ENABLED=false)
#    - Real email mode: Actual emails sent (VITE_EMAIL_ENABLED=true)
#    - VITE_ prefix required for Vite to expose to client-side code
#    - VITE_APP_URL: Base URL of your app for generating reset links
#      * Development: http://localhost:3000
#      * Production: https://yourdomain.com or https://your-netlify-app.netlify.app
#
# 3. SECURITY:
#    - Never commit .env file to version control
#    - Keep credentials secure and private
#    - Rotate passwords regularly
#    - Use different credentials for dev/prod
#
# 4. GMAIL LIMITS:
#    - Free Gmail: ~500 emails/day
#    - Google Workspace: ~2000 emails/day
#    - Sufficient for most cafe operations
#
# 5. SMS:
#    - Optional for development (set VITE_SMS_ENABLED=false)
#    - Required for production customer OTP verification (set VITE_SMS_ENABLED=true)
#    - Console logging mode: SMS logged to console (VITE_SMS_ENABLED=false)
#    - Real SMS mode: Actual SMS sent via webhook (VITE_SMS_ENABLED=true)
#    - Recommended for Sri Lanka: Dialog Ideabiz SMS Gateway
#    - International options: Twilio, Vonage/Nexmo
#    - VITE_ prefix required for Vite to expose to client-side code
#
# 6. STRIPE PAYMENTS:
#    - Optional - can use bank transfer only without Stripe
#    - Required for online card payments (set VITE_STRIPE_ENABLED=true)
#    - Test mode: Use pk_test_* and sk_test_* keys for development
#    - Production mode: Use pk_live_* and sk_live_* keys for production
#    - Get keys from: https://dashboard.stripe.com/apikeys
#    - Webhook secret from: https://dashboard.stripe.com/webhooks
#    - STRIPE_SECRET_KEY should only be in Netlify environment variables (not exposed to client)
#    - VITE_STRIPE_PUBLISHABLE_KEY is safe to expose in client-side code
#
# ============================================================================
