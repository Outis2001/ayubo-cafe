# ============================================================================
# AYUBO CAFE - ENVIRONMENT VARIABLES
# ============================================================================
#
# Copy this file to `.env` and update with your actual values
# Command: cp env.example .env
#
# ⚠️ IMPORTANT: Never commit .env file to git (already in .gitignore)
#
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Example:
# VITE_SUPABASE_URL=https://abcdefghijklm.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# EMAIL CONFIGURATION (Optional - for password reset emails)
# ============================================================================
# Leave blank to use development mode (emails logged to console)
# Configure for production to send actual emails

# ===== Gmail SMTP (Recommended) =====
# Follow setup guide: EMAIL_SETUP_GUIDE.md

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-character-app-password
EMAIL_FROM="Ayubo Cafe <your-email@gmail.com>"

# Debug mode: Set to 'true' to see full email content in console
EMAIL_DEBUG=false

# Enable email sending (set to 'false' for dev mode - emails logged to console only)
EMAIL_ENABLED=false

# Application URL (used for generating password reset links)
# Development: http://localhost:3000
# Production: https://yourdomain.com
APP_URL=http://localhost:3000

# ===== Alternative SMTP Services =====

# SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASSWORD=your-sendgrid-api-key

# Mailgun:
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-mailgun-username
# EMAIL_PASSWORD=your-mailgun-password

# AWS SES:
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-ses-smtp-username
# EMAIL_PASSWORD=your-ses-smtp-password

# ============================================================================
# NOTES
# ============================================================================
#
# 1. SUPABASE:
#    - Required for database operations
#    - Get credentials from Supabase dashboard
#    - VITE_ prefix required for Vite to expose to client
#
# 2. EMAIL:
#    - Optional for development (set EMAIL_ENABLED=false)
#    - Required for production password reset (set EMAIL_ENABLED=true)
#    - Gmail setup: See EMAIL_SETUP_GUIDE.md
#    - Development mode: Emails logged to console (EMAIL_ENABLED=false)
#    - Production mode: Actual emails sent (EMAIL_ENABLED=true)
#    - APP_URL: Base URL of your app for generating reset links
#      * Development: http://localhost:3000
#      * Production: https://yourdomain.com or https://your-netlify-app.netlify.app
#
# 3. SECURITY:
#    - Never commit .env file to version control
#    - Keep credentials secure and private
#    - Rotate passwords regularly
#    - Use different credentials for dev/prod
#
# 4. GMAIL LIMITS:
#    - Free Gmail: ~500 emails/day
#    - Google Workspace: ~2000 emails/day
#    - Sufficient for most cafe operations
#
# ============================================================================
